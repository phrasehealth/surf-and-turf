# Data notes for the agent

Replace with your local README.md (data dictionary, naming conventions, known
data-quality issues, preferred metrics definitions).

The agent reads this file, `CLAUDE.md`, and the `transforms/` repository with the
Read/Glob/Grep tools before querying.
