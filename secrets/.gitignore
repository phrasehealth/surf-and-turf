# Nothing in this directory is ever committed -- it holds private keys.
# This file lives inside the directory it protects so the rule survives
# edits to the root .gitignore.
*
!.gitignore
!README.md
